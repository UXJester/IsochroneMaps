# Path for Source Code
SRC = "src"

# Path for Data (remains outside /src)
DATA = "data"
ISOCHRONES = f"{DATA}/isochrones"
LOCATIONS = f"{DATA}/locations"

# Path for Maps (remains outside /src)
MAPS = "maps"

# Configuration for static images (remains outside /src)
IMAGES = "public/images"
